@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if(@LIBUSB_CONFIG_NEEDS_THREADS@)
    find_dependency(Threads)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/libusb-targets.cmake")
check_required_components(libusb)
